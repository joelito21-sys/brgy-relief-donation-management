<?php

use Illuminate\Support\Facades\Route;

// Default user authentication routes (if needed)
// This file can be used for general user authentication in the future

// For now, authentication routes are handled in:
// - donor_routes.php (for donor authentication)
// - admin.php (for admin authentication) 
// - resident_routes.php (for resident authentication)

// Example placeholder for future user authentication
// Route::middleware('guest')->group(function () {
//     Route::get('login', [AuthenticatedSessionController::class, 'create'])->name('login');
//     Route::post('login', [AuthenticatedSessionController::class, 'store']);
// });
